# classify

## Job

Default Kody entry point for issue execution.

## Allowed Commands

- Use only the tools needed to complete the capability's purpose.

## Restrictions

- Stay within the capability's purpose and the per-executable rules.
- Do not perform actions outside the contract defined by this capability.
- If this capability needs to produce a report, run the configured report executable instead of writing report files directly.
